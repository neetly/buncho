{
  "name": "@buncho/dotenv",
  "version": "0.5.0",
  "homepage": "https://github.com/BunchoDev/buncho",
  "repository": {
    "type": "git",
    "url": "git@github.com:BunchoDev/buncho.git",
    "directory": "packages/core/dotenv"
  },
  "license": "MIT",
  "main": "./src/index.js",
  "files": [
    "./src"
  ],
  "scripts": {
    "build": "run --top-level lib build",
    "clean": "run --top-level lib clean",
    "prepack": "run clean && run build && cp ../../../LICENSE ."
  },
  "dependencies": {
    "cosmiconfig": "^7.0.0",
    "dotenv": "^8.6.0",
    "dotenv-expand": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.43"
  }
}
